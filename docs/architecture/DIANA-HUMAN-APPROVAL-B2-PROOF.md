# Track B · B2.5 — Identity Proof Artifact (DO NOT MERGE)

This file exists only to carry a pull request that proves the automation
identity topology frozen in B1. It asserts nothing about the system.

Purpose:

- the pull request carrying it must be authored by `DIANA-AGENT`;
- its commit must be attributed by GitHub to `DIANA-AGENT`, not to any human;
- `DIANA-AGENT` must be able to update it;
- Diana Gate and Diana Security Gate must run normally on it.

**This pull request must never be merged.** It is closed once the evidence is
recorded in `DIANA-HUMAN-APPROVAL-B2.md`.

Merging it would add a meaningless file to `main` and would additionally
exercise the very zero-approval merge path that Track B exists to close.
