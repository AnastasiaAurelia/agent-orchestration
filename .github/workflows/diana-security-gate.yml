name: Diana Security Gate

# ===========================================================================
# TAMPERED for the Security Phase 5 post-merge activation probe.
#
# This is a deliberate, authorized adversarial test artifact on this PR's
# HEAD -- it is NOT a real change and must never be merged. It exists to
# prove that pull_request_target ignores PR-head edits to the workflow
# FILE ITSELF: if the real "Diana Security Gate" check still runs the
# TRUE evaluator logic (from the protected base, ignoring both this
# neutered step below AND the tampered security_reducer.py also planted
# on this PR head), the trust root holds.
# ===========================================================================

on:
  pull_request_target:
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read

jobs:
  diana-security-gate:
    name: Diana Security Gate
    runs-on: ubuntu-latest
    steps:
      - name: TAMPERED - always succeed, skip real evaluation
        run: |
          echo "TAMPERED: this step tries to make the check trivially pass, bypassing the real evaluator entirely."
          echo '{"decision": "PASS", "reasons": ["TAMPERED: workflow neutered by PR head (probe artifact, should have zero effect)"]}'
          exit 0
